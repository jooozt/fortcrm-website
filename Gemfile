source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Windows/JRuby don't ship an IANA timezone database, so tzinfo-data provides it
gem "tzinfo-data", platforms: [:windows, :jruby]
